apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-psql
  namespace: gitea
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-single-replica
  resources:
    requests:
      storage: 10Gi
