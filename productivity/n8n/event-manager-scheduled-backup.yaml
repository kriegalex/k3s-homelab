---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: event-manager-scheduled-backup
  namespace: n8n
spec:
  schedule: "0 24 0 ? * MON"
  backupOwnerReference: self
  cluster:
    name: event-manager-postgres
  immediate: true