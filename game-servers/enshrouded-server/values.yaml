# enshrouded-server — chart: enshrouded-server v1.0.6.
# Real deployed values (source of truth for `helm upgrade -f values.yaml`).
# The server password is PII/credential — kept out of git. It is layered at
# deploy time from the gitignored fragment:
#   helm upgrade --install enshrouded-server <chart> -n game-servers \
#     -f values.yaml -f helm-values-secret.yaml
enshrouded:
  maxPlayers: 8
  serverName: Ste ApoLAN Server
  serverPassword: ""   # real value in helm-values-secret.yaml (gitignored)
persistence:
  enabled: true
  size: 10Gi
resources:
  limits:
    cpu: 3000m
    memory: 8Gi
  requests:
    cpu: 1500m
    memory: 4Gi
