apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: palworld-server
  namespace: game-servers
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
