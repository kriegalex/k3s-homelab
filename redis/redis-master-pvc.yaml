apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-master
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-single-replica  # Use single-replica for cache data
  resources:
    requests:
      storage: 8Gi
